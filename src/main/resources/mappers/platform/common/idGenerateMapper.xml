<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.romy.platform.main.common.mapper.InGenerateMapper">

    <select id="selectSeqNextId" resultType="java.lang.String">
        /* selectSeqNextId : Seq 값 조회 */
        SELECT FORMAT(NEXT_ID, 'D20')
          FROM PLT_SEQ
         WHERE TABLE_NAME = 'COMMON_ID'
    </select>

    <update id="updateSeqNextId">
        /* updateSeqNextId : NextId 값 증가 */
        UPDATE PLT_SEQ
           SET NEXT_ID = NEXT_ID + 1
         WHERE TABLE_NAME = 'COMMON_ID'
    </update>

</mapper>